there are 2 file are important 
1. test file
2. config file (test runner that run all the test that u create in test file) 


// what does npx playwright test
// npx serch the command inside the node module which is playwright then playwright internally start the config file which run all the testcases